# Синхронизация с upstream llama.cpp - 2026-09-05

## 1. Итог

Выполнен merge `refs/remotes/upstream/master` (4d9176092) в ветку `master`.

- merge-коммит: `57e050001` (родители `413cfde3b` = локальный master, `4d9176092` = upstream)
- upstream HEAD: `4d9176092` "sycl : fix test-backend-ops CI break && restore Kronecker product FWHT support (#28016) (#28254)", 2026-09-05 10:37 +0800
- merge-base до слияния: `8887a48f0`
- подтянуто: 75 коммитов (73 non-merge + 2 merge), behind стал 0
- наших уникальных коммитов осталось: 25 (24 патча + merge-коммит)
- конфликтов слияния: 0 (предпроверка `git merge-tree --write-tree`, merged tree `5099c94bb`; сам merge прошёл без конфликтов)
- рабочее дерево после merge: чистое, кроме untracked `_optimizations/`, `$null`, `ERR`

## 2. Откат

Бэкап-ветка `backup/pre-sync-20260905` = `413cfde3b`.

```powershell
git reset --hard backup/pre-sync-20260905
```

## 3. Сборка после слияния

Re-configure прошёл in-place, чистка CMakeCache не потребовалась.

- вычищена устаревшая кэш-запись `GGML_CUDA_PEER_MAX_BATCH_SIZE:STRING=128` (опцию удалил upstream `24f5bf8a4`): `cmake -U "*PEER_MAX_BATCH*" build_avx_512`
- потребовался re-configure из-за перехода на configure_file: новые `ggml/src/ggml-version.h.in` (`86b351fd6`) и `src/llama-version.h.in` (`d230ddd76`)
- cl.exe отсутствует в обычном PATH, операции выполнялись через `vcvars64.bat` (VS 2022 BuildTools, MSVC 14.44.35207); CUDA 13.3 (nvcc V13.3.33), ninja 1.13.2, cmake 4.2.0-rc1
- опции подтверждены: `GGML_NATIVE=ON`, `GGML_LTO=ON`, `GGML_OPENMP=ON`, `GGML_CUDA_FA=ON`, `GGML_CUDA_FA_ALL_QUANTS=ON`, `GGML_CUDA_GRAPHS=ON`, `CMAKE_CUDA_ARCHITECTURES=86`, `GGML_SCHED_MAX_COPIES=1`, AVX512/AVX512_VBMI/AVX512_VNNI/AVX_VNNI/AVX2/BMI2 = ON
- `llama-server`: сборка успешна, [496/496], exit 0, ~7.5 мин (с ccache), `build_avx_512/bin/llama-server.exe` 79 760 384 байт
- `llama-bench`: сборка успешна, `build_avx_512/bin/llama-bench.exe` 75 364 864 байт
- ошибок компиляции: 0; warning'ов по файлам наших патчей (norm.cu, fattn.cu, ggml-cuda.cu, qwen4exp.cpp, iqp.cpp, quants.c, FindSIMD.cmake, ggml.c): 0
- новый upstream-файл скомпилировался: `[55/498] Building CUDA object .../moe-weighted-reduction.cu.obj`
- нефатально на configure: не скачались UI-ассеты и HF (`Failure when receiving data from the peer`) - сеть, на компиляцию llama-server не влияет; NCCL не найден (штатно для одной GPU)

Логи сборки: `D:\dev\llama_compiling\_forks_opt\sync_exec\build_log.txt`, `build_bench_log.txt`.

## 4. Статус наших 24 локальных патчей после merge

| Патч | Файл | Статус |
|---|---|---|
| fused rms_norm+mul+add+rms_norm (CUDA) | `ggml/src/ggml-cuda/norm.cu`, `norm.cuh`, `ggml-cuda.cu` | ВЫЖИЛ (`rms_norm_mul_add_rms_norm_f32` x4, `ggml_cuda_op_rms_norm_mul_add_rms` x1, хук в ggml-cuda.cu; в upstream этих символов 0) |
| FA mma f16 ncols2=4 для head256/gqa12 | `ggml/src/ggml-cuda/fattn.cu:224-228` | ВЫЖИЛ (условие `use_gqa_opt && DKQ == 256 && gqa_ratio == 12 && Q->ne[1] > 2`, blame `8377362c7`) |
| small F32 GEMM fast path | `ggml/src/ggml-cuda/ggml-cuda.cu:1818`, `1848-1859` | ВЫЖИЛ (kernel `k_simple_gemm_f32`; сигнатура `ggml_cuda_mul_mat` upstream не менялась) |
| QSA skip indexer scoring | `src/models/qwen4exp.cpp:550`, `576`, `601` | ВЫЖИЛ (`fits_top_k = n_kv <= hparams.indexer_top_k`; blame `1e49846d25`) |
| fast tensor name formatting | `ggml/src/ggml.c`, `src/llama-context.cpp:2524-2543` | ВЫЖИЛ (`ggml_format_name_fast` x14; ручной memcpy+цифровой suffix вместо snprintf) |
| NUMA fallback | `ggml/src/ggml-cpu/iqp.cpp:1220-1221` | ВЫЖИЛ ЧЕРЕЗ UPSTREAM (upstream получил идентичный код, blob == upstream) |
| AVX-VNNI MSVC detect (PR-27590) | `ggml/src/ggml-cpu/cmake/FindSIMD.cmake:43`, `107-111` | ВЫЖИЛ (+19 строк относительно upstream, в upstream нет) |
| AVX512/VNNI Q5_K/Q6_K (PR-25346) | `ggml/src/ggml-cpu/arch/x86/quants.c` | ВЫЖИЛ (+235/-20 относительно upstream, ровно объём `b4b85ef9a`) |
| IQP (PR-27402) | `ggml/src/ggml-cpu/iqp.cpp`, `iqp.h` | СХЛОПНУЛОСЬ С UPSTREAM (upstream-коммит `85c55223c`, дубликатов нет, `git diff` пуст) |

## 5. Релевантное для производительности из подтянутого

- `8e93a9773` sparse-fa для DSV4/GLM: CUDA + ggml sparse flash-attention, добавляет `ggml_flash_attn_ext_set_n_kv_max` и параметр `int64_t n_kv_max` в `build_attn_mha`
- `3466812d1` fused MoE weighted expert reduction: новый `ggml/src/ggml-cuda/moe-weighted-reduction.cu/.cuh`
- `8c1a25166` MMVQ->MMQ crossover для SM87: перенастройка порога переключения mul_mat_vec_q на mul_mat_q (на SM86 эффект косвенный, пороги общие)
- `0ba6499c3` concurrent streams per split: параллельные CUDA-стримы при разделении задач на несколько GPU (у нас одна GPU - неактивно)
- `b356fa262` kv-cells: поиск n-gram истории через индекс позиций последовательности вместо линейного скана

Прочие группы: металл/SYCL/Vulkan/OpenCL/Hexagon-тюнинги (~25), новые архитектуры (hy_v4, NemotronHPuzzle, qwen3-tts fix и др., ~8), mtmd/visual (~4), server/UI/common (~8), docs/ci/scripts (~15), тесты и мелкие фиксы (~10).

## 6. QSA skip против upstream sparse-fa: вердикт SKIP-ORTHOGONAL

- Цепочка FA для qwen4exp: `build_layer_attn()` (`src/models/qwen4exp.cpp:778`) -> `build_qsa_top_k()` (`:531`, при `fits_top_k` возвращает nullptr на `:601`) -> либо обычный `build_attn` -> `build_attn_mha()` (`src/llama-graph.cpp:2736`), либо `build_attn_qsa()` (`:693`) -> `build_attn_mha()` (`src/models/qwen4exp.cpp:767`). В ОБОИХ ветках `n_kv_max = 0` (альтернатива с `top_k->ne[0]` закомментирована на `:766` и не используется). Внутри `build_attn_mha()` строка `src/llama-graph.cpp:2586` безусловно вызывает `ggml_flash_attn_ext_set_n_kv_max(cur, 0)`; в `ggml/src/ggml.c:5523` это `op_params[4]`, дефолт 0 (`ggml_new_tensor_impl` обнуляет op_params), комментарий API: "Set 0 to disable".
- Значит для qwen4exp sparse-fa ВЫКЛЮЧЕН. Сам upstream-коммит `8e93a9773` тронул `qwen4exp.cpp` ровно в одной строке - добавил `0` в новую сигнатуру.
- Условие CUDA-dispatch sparse (`ggml/src/ggml-cuda/fattn.cu:109`, `124-128`): NVIDIA + turing-mma + `mask != nullptr` + `n_kv_max > 0` + без ALiBi + без softcap + `mask->ne[0]==K->ne[1]`, `mask->ne[2]==1` + `K->ne[1] >= max(4096, 2*n_kv_max)`. Compile-time гейт (`ggml/src/ggml-cuda/fattn-mma-f16.cuh:1758`): sparse-инстансы есть ТОЛЬКО для `(DKQ=512, DV=512, ncols2=8)` и `(DKQ=576, DV=512, ncols2=16)`.
- Порядок dispatch (`ggml/src/ggml-cuda/fattn.cu`): `ggml_cuda_flash_attn_ext()` (`:709`) -> `ggml_cuda_get_best_fattn_kernel()` (`:497`; decode `Q->ne[1]==1` обычно уходит в VEC, где sparse нет) -> `ggml_cuda_flash_attn_ext_mma_f16()` (`:252`) -> `case 256` (`:297-300`) -> `switch_ncols2<256,256>()` (`:170`): Volta special-case (`200-222`), затем НАШ блок (`224-228`), затем generic gqa-ветки (`230`/`235`/`240`). Sparse-перехват находится ВНУТРИ `switch_ncols1()` (`:133`, строки `138-143`) и для инстанса 256/256/4 не компилируется вообще; DSV4/GLM с `case 576` (`:321+`) идут напрямую в `switch_ncols1`, минуя наш блок. Взаимного перехвата нет ни до, ни после.
- Кто реально использует nonzero `n_kv_max`: только `src/models/deepseek4.cpp:760-761` и overload `build_attn` с `top_k` (`src/llama-graph.cpp:3011`), который зовут `deepseek32.cpp:400`, `glm-dsa.cpp:444`, `dots3note.cpp:383`, `hy-v4.cpp:483`. qwen4exp в этом списке нет.
- (а) наш skip по-прежнему даёт эффект: ни одна новая механика upstream не пропускает построение индексатора и `ggml_top_k`; upstream лишь ускорял бы сам FA поверх уже готовой маски и только на DKQ 512/576
- (б) корректность: комбинация не может дать неверный результат для qwen4exp - sparse-ветка недостижима (`ggml/src/ggml-cuda/fattn.cu:126`, для Metal `ggml/src/ggml-metal/ggml-metal-ops.cpp:2869`); путь `kq_mask_top_k` не менялся
- (в) правка не требуется. Опциональный кандидат на ЗАМЕР (не на правку): передать `top_k->ne[0]` вместо 0 в `src/models/qwen4exp.cpp:767` - выгоду это могло бы дать только на Metal (там DKQ=256 в white-list и нужен hint <= 4096), на CUDA бесполезно

## 7. Риски и что ещё не проверено

- Runtime-корректность CUDA-ядер (FA + FA_ALL_QUANTS, moe-weighted-reduction, CUDA Graphs) компиляцией не гарантируется - нужен прогон модели, проверка на NaN/OOM под 10 GB VRAM
- Численная эквивалентность генерации относительно сборки до merge (патчи `norm.cu`, `iqp.cpp`, `quants.c`) не проверялась
- qwen4exp на реальном GGUF не проверялась
- Производительность не измерялась. Базовая команда: `.\build_avx_512\bin\llama-bench.exe -m <model> -p 512,2048,8192 -n 128 -pg`, 3 прогона, медиана; сравнение с логами сборки до merge
- `76ad2ba9f` и идентификаторы `CL-*` из аудита форков не являются git-объектами этого репозитория, их наличие в upstream git-ом проверить нельзя
- PR-25346 и PR-27590 в upstream отсутствуют (подтверждено поиском по истории upstream); официальные статусы PR на GitHub не открывались
- В репозитории работает фоновый авто-fetch upstream (примерно раз в 15 минут), поэтому `.git/FETCH_HEAD` перезаписывается; авторитетная точка - `refs/remotes/upstream/master`
- Мусорные untracked файлы `$null` и `ERR` в корне репозитория оставлены как были (не создавались в ходе этой работы); удалить можно вручную

## 8. Запреты, соблюдённые при синхронизации

`git push`, `gh pr create`, `git rebase`, `git reset --hard`, `git clean`, запись AI-сообщений коммитов (merge выполнен с `--no-edit`, сообщение сгенерировано git), изменение исходников и `_optimizations/` - не выполнялись.

## 9. Итерация 2: до-слив (2026-09-05, 11:5x UTC)

- merge `46b388074` (родители `57e050001` + `6a1a922d2`), сообщение сгенерировано git через `--no-edit`, конфликтов 0
- подтянуто 1 коммит: `6a1a922d2` "metal : fix memory leak in early return (#28399)" от 2026-09-05 12:19:47 +0200; diff 57e050001..HEAD = 1 файл / 1 строка (`ggml/src/ggml-metal/ggml-metal-context.m`)
- за прошлый merge (57e050001) было влито ВСЁ до `4d9176092`; фильтр по релевантности НЕ применялся ни разу - в both случаях вливалась вся ветка целиком
- пересборка: re-configure + `llama-server` и `llama-bench` собраны успешно, exit 0, ~1 мин каждая инкрементально, ошибок 0, warning'ов по файлам локальных патчей 0 (единственный warning компилятора: C4297 в `src/llama.cpp:75`, upstream-код)
- бинари: `build_avx_512/bin/llama-server.exe` 79 760 384 байт (05.09.2026 14:40:43), `build_avx_512/bin/llama-bench.exe` 75 364 864 байт (05.09.2026 14:41:36) локальное время UTC+3
- верификация локальных патчей после merge: все ВЫЖИЛИ (norm.cu 4 вхождения `rms_norm_mul_add_rms_norm_f32`; хит `DKQ == 256 && gqa_ratio == 12` в `fattn.cu:224-228`; `k_simple_gemm_f32` x2; `fits_top_k` x3; `ggml_format_name_fast` x14; `AVX_VNNI_CODE` x1; `quants.c` +235/-20 относительно upstream; `iqp.cpp/.h` идентичны upstream). Ни один из 12 файлов с локальными патчами не был затронут подтянутым коммитом.
- откат только этого merge: `git reset --hard backup/pre-sync2-20260905`; полный откат ко дну до синхронизации: `git reset --hard backup/pre-sync-20260905`

## 10. Подтверждение полноты относительно живого upstream (2026-09-05, 12:12-12:18 UTC)

- причина сетевых падений: переменная окружения `https_proxy=http://191.101.126.19:50101`, прокси сбрасывает CONNECT-туннель к github.com:443 (`curl -v`: `Recv failure: Connection was reset`, curl code 56); конфиг git не менялся, использованы только одноразовые флаги `-c http.proxy= -c https.proxy=`
- попытки `ls-remote`:
  1. `git ls-remote upstream refs/heads/master` -> fatal: unable to access 'https://github.com/ggml-org/llama.cpp.git/': Recv failure: Connection was reset
  2. `git -c http.version=HTTP/1.1 ls-remote upstream refs/heads/master` -> та же ошибка
  3. `git -c http.postBuffer=524288000 ls-remote upstream refs/heads/master` -> та же ошибка
  4. `git ls-remote https://github.com/ggml-org/llama.cpp.git refs/heads/master` -> та же ошибка
  5. `git -c http.proxy= -c https.proxy= ls-remote upstream refs/heads/master` -> OK (пустой прокси переопределил переменные окружения); sha живого upstream = `6a1a922d269908a29cbd4b49c27e6a8e7fd10fae`
- `git -c http.proxy= -c https.proxy= fetch --no-tags upstream master` -> успешно (`master -> FETCH_HEAD`), FETCH_HEAD = `6a1a922d269908a29cbd4b49c27e6a8e7fd10fae`
- локальный снимок `refs/remotes/upstream/master` = `6a1a922d269908a29cbd4b49c27e6a8e7fd10fae` - СОВПАДАЕТ с живым upstream, расхождений нет; reflog снимка: последний fast-forward 2026-09-05T13:30:13+03:00 (10:30 UTC)
- HEAD = `46b388074`; behind = `git rev-list --count HEAD..refs/remotes/upstream/master` = 0 относительно ЖИВОГО upstream; наших собственных коммитов (`--left-only HEAD...refs/remotes/upstream/master`) = 26
- merge итерации 3 НЕ потребовался и НЕ делался (behind = 0); пересборка не требовалась
- повторный `git ls-remote` без переопределения прокси снова падает (Connection was reset) - прокси нестабилен, проверка полноты должна использовать одноразовые флаги пустого прокси
- верификация локальных патчей на HEAD `46b388074` (повторно, после подтверждения refs): `rms_norm_mul_add_rms_norm_f32` x4 (`norm.cu`); условие `use_gqa_opt && DKQ == 256 && gqa_ratio == 12 && Q->ne[1] > 2` в `fattn.cu:225` (блок 224-228); `k_simple_gemm_f32` x2 (`ggml-cuda.cu`); `fits_top_k` x3 (`qwen4exp.cpp`); `ggml_format_name_fast` x14 (`ggml.c`); `AVX_VNNI_CODE` x1 (`FindSIMD.cmake`); `quants.c` +235/-20 относительно upstream; diff `iqp.cpp`/`iqp.h` относительно upstream пуст
- команда ручной проверки полноты в любой момент: `git -c http.proxy= -c https.proxy= fetch --no-tags upstream master ; git rev-list --count HEAD..refs/remotes/upstream/master`
